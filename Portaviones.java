package BatallaNaval;

import java.util.ArrayList;

public class Portaviones extends Ficha {
    // Llamada al constructor de la clase padre
    public Portaviones(ArrayList<Integer> posicion, String estado) {
        super(4, posicion, estado);
    }
}
